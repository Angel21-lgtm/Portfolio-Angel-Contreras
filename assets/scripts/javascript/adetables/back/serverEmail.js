"use strict";

import { values } from "../contact/submitData";

const serverEmail = () => {
    const http = require("../../library/node/node_modules/node/http");
    http.createServer((values));
}

export {
    serverEmail
}